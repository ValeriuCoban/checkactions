name: Node CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1      
      with:
        fetch-depth: '0'
    - name: Bump version and push tag
      uses: valeriucoban/github-tag-action
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        WITH_V: true
    - name: npm install, build, and test
      run: |
        echo $GITHUB_WORKSPACE
        pwd 
        ls -l
      env:
        CI: true
